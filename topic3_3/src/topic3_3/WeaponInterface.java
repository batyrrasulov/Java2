package topic3_3;

public interface WeaponInterface {
	//add method that returns void and takes no arguments
	public void fireWeapon();
	//add method that returns void and takes a power argument as an integer type
	public void fireWeapon(int power);
	//add method that returns void and takes an argument as a boolean type
	public void activate(boolean enable);
}
